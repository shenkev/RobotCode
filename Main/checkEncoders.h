#ifndef _CHECK_ENCODERS_H
#define _CHECK_ENCODERS_H

/**
This is the function called to check the encoders to see how fast the robot is actually moving. 
Converting the raw reading into a speed is probably not necessary.
Might be simplest to just return true for stuck and false for not stuck and have a threshold for "being stuck".
**/

//input:
//output:
//function calls:
//purpose:
inline boolean checkEncoders()
{
}

#endif